# Sjekker om modulus er identisk for crt og key for SSL sertifikat
# Bytt ut 'a' med navnet på siten.
SITE=a     && CRT=$(openssl x509 -in crt/$SITE -noout -modulus) && KEY=$(openssl rsa -in key/$SITE -noout -modulus) && diff $CRT $KEY
